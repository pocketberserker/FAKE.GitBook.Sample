branches:
  except:
    - gh-pages

skip_tags: true

skip_commits:
  message: \[(ci skip|skip ci)\].*

clone_depth: 50

install:
  - ps: Install-Product node ''
  - npm install
  - choco install calibre

build_script:
  - cmd: build.cmd

environment:
  access_token:
    secure: /RXusYkOTtsXn3OKWHg65Tbj8rasEzKbyMS+qGqCjqdk+Hcsu1CKYcUJ0hvd1Shd

on_success:
  - git config --global credential.helper store
  - ps: Add-Content "$env:USERPROFILE\.git-credentials" "https://$($env:access_token):x-oauth-basic@github.com`n"
  - git config --global user.name "pocketberserker"
  - git config --global user.email "pocketberserker@gmail.com"
  - ps: if(($env:APPVEYOR_REPO_BRANCH -eq "master") -And (-Not $env:APPVEYOR_PULL_REQUEST_NUMBER)) { ./build.cmd Release  }

